# Rust build artifacts
/target
**/target
/build/
# Library crate: consumers resolve their own versions. The compat-gate CI
# job also depends on a fresh cargo generate-lockfile each run.
Cargo.lock

# mdBook output
book/book/

# Dear ImGui runtime settings cache, written to cwd by any local run of an
# ImGui-context test/example/tool.
/imgui.ini

# Editor / OS cruft
.DS_Store
*.swp
.idea/
.vscode/
